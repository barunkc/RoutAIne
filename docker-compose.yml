version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: routaine-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: routaine
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
